server {
  listen <% if @force_https %>443<% else %>80<% end %>;
  server_name <%= node['hostname'] %>;
  
  access_log  <%= node['nginx']['log_dir'] %>/maint.access.log;

  location /test {
    return 404;
  }
}
